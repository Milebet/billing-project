from django.urls import path
from . import views

urlpatterns = [
	#path('register_customer/',views.RegisterCustomer, name='register_customer'),
	#path('index_customers/',views.IndexCustomers, name='index_customers'),
	#path('generate_invoice/',views.GenareteInvoice, name='generate_invoice'),
	#path('search_invoice/',views.SearchInvoice, name='search_invoice'),
]